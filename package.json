{
  "name": "es2018-and-beyond-talk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "concurrently": "^3.5.1",
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.1",
    "gulp-sourcemaps": "^2.6.4",
    "gulp-tap": "^1.0.1",
    "http-server": "^0.11.1",
    "rimraf": "^2.6.2"
  },
  "scripts": {
    "build": "gulp",
    "build:watch": "gulp && gulp:watch",
    "clean": "gulp clean",
    "serve": "gulp && http-server -o",
    "serve:watch": "concurrently \"gulp && gulp watch\" \"http-server -o\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
